# How to add two numbers

# Store input numbers randomly
num1 = input('first number: ')
num2 = input('second number: ')

# Add two numbers
sum = float(num1) + float(num2)

# Display the sum
print('The sum of {0} and {1} is {2}'.format(num1, num2, sum))
